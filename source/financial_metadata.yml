TA:
  Close:
    required_fields: ["Date", "Close"]
  MA:
    required_fields: ["Date", "Close"]
  DailyReturn:
    required_fields: ["Date", "Close"]
  CumulativeReturn:
    required_fields: ["Date", "Close"]
  Volume:
    required_fields: ["Date", "Volume"]
  HighLowRange:
    required_fields: ["Date", "High", "Low"]
  CorrelationMatrix:
    required_fields: ["Date", "Close", "Ticker"]

FA:
  MarketCap:
    required_fields: ["symbol", "market_cap"]
  PE:
    required_fields: ["symbol", "pe_ratio"]
  SectorDistribution:
    required_fields: ["symbol", "sector"]
  MarketCap_vs_PE:
    required_fields: ["symbol", "market_cap", "pe_ratio"]
  AvgVolume_vs_AvgClose:
    required_fields: ["symbol", "avg_volume", "avg_close"]
  TotalDividends:
    required_fields: ["symbol", "dividends"]
